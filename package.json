{
  "name": "bq-write-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "cli": "npm run dev --workspace=apps/cli",
    "web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
