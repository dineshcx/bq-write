# For local development only — copy to .env
# When installed globally via npm, set these in ~/.zshrc or ~/.bashrc instead:
#   export ANTHROPIC_API_KEY=sk-ant-...

ANTHROPIC_API_KEY=sk-ant-...
BQ_MAX_RESULTS=100
CONTEXT_MAX_TOKENS=80000
